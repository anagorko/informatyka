#include <iostream>
using namespace std;

int main(){
	int n;		//liczba cząsteczek
	cin >> n;
	int t[n-1];		//kolejne prędkości cząstek
	for(int i=0; i<n; i++){		//wpisywanie do tablicy
		int j;		//liczba, która zaraz będzie wpisana
		cin >> j;
		t[i]=j;
	}
	int q;		//liczba zapytań
	cin >> q;
	for(; q>0; q--){
		int p;		//zapytanie
		cin >> p;
		int a;		//min indeks
		int b;		//max indeks
//wyznaczanie a
		int as=0;		//początek
		int ae=n-1;		//koniec
		int ax=(ae+as)/2;		//sprawdzany indeks
		for(;;){
		}
//wyznaczanie b
		int bs=0;		//początek
		int be=n-1;		//koniec
		int bx=(be+bs)/2;		//sprawdzany indeks
		for(;;){
		}
		a=b-a;
		cout << a << endl;
		/*for(;;){		//wyszukiwanie binarne 1
			if(t[x]>p){		//jeśli x jest za duży
				e=x-1;
				x=(e+s)/2;
			}
			if(t[x]<p){		//jeśli x jest za mały
				s=x+1;
				x=(e+s)/2;
			}
			if(t[x]==p){		//jeśli x trafi
				for(;;){		//wyszukiwanie binarne 1.1
					if(t[x]>p){		//jeśli x jest za duży
						e=x-1;
						x=(e+s)/2;
					}
					if(t[x]<p){		//jeśli x jest za mały
						s=x+1;
						x=(e+s)/2;
					}
					if(){}
				}
				break;
			}
		}*/
	}
	return 0;
}
